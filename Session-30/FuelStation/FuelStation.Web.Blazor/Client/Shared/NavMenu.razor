@inject HttpClient httpClient
@inject NavigationManager navigationManager
@inject IJSRuntime jsRuntime

    <div hidden>
        <p>@person</p>
    </div>
    <div class="top-row ps-3 navbar navbar-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="">Fuel Station</a>
            <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
                <span class="navbar-toggler-icon"></span>
            </button>
        </div>
    </div>

    <div class="@NavMenuCssClass nav-scrollable" @onclick="ToggleNavMenu">
        <nav class="flex-column">
            <div class="nav-item px-3">
                <NavLink class="nav-link" href="MainLayout">41
                    <span class="oi oi-home" aria-hidden="true"></span> Login Page
                </NavLink>
            </div>
            <br />
            @if (person == 'c' | person == 'm' ){
                <div class="nav-item px-3">
                    <NavLink class="nav-link" href="CustomerList">
                        <span class="oi oi-cart" aria-hidden="true"></span> Customers
                    </NavLink>
                </div>
            }
            @if (person == 'm') { 
                <div class="nav-item px-3">
                    <NavLink class="nav-link" href="EmployeeList">
                        <span class="oi oi-people" aria-hidden="true"></span> Employees
                    </NavLink>
                </div>
            }
            @if (person == 's' | person == 'm') {
                <div class="nav-item px-3">
                    <NavLink class="nav-link" href="Itemlist">
                        <span class="oi oi-tags" aria-hidden="true"></span> Items
                    </NavLink>
                </div>
            }
            <div class="nav-item px-3">
                <NavLink class="nav-link" href="">
                    <span class="oi oi-book" aria-hidden="true"></span> Ledger
                </NavLink>
            </div>
        </nav>
    </div>
@*}*@

@code {

    
    [Parameter]
    public char person { get; set; }
     
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
